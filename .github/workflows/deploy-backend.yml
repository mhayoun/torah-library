name: Deploy Backend
on:
  push:
    branches: [master]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: |
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_Qb5NBnRnPFrYZV7piJKSwXh4fn6G/1Ig5ZMydRA"
